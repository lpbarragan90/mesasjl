rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /plans/{planId} {
      // REGLAS DE PRUEBA: permiten lectura/escritura a cualquiera
      // No usar en producción sin restricciones
      allow read, write: if true;
    }
  }
}